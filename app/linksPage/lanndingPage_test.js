'use strict';

describe('myApp.linksPage module', function() {

  beforeEach(module('myApp.links'));

  describe('linksPage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});